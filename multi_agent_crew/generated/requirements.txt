# =============================================================================
# requirements.txt
# retry 装饰器项目的依赖声明
#
# 安装方式:
#   pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# 运行时依赖 (Runtime dependencies)
# -----------------------------------------------------------------------------
# retry 装饰器本身仅使用 Python 标准库 (functools / time / logging),
# 无第三方运行时依赖,故此处为空。
# 要求 Python >= 3.8

# -----------------------------------------------------------------------------
# 测试依赖 (Test dependencies)
# -----------------------------------------------------------------------------
# pytest: 单元测试框架,用于运行附带的所有测试用例
pytest>=7.4.0,<9.0.0

# pytest-cov: 测试覆盖率统计插件 (可选,但推荐)
# 使用方式: pytest --cov=retry_decorator --cov-report=term-missing
pytest-cov>=4.1.0,<6.0.0

# pytest-mock: 提供 mocker fixture,便于统计被装饰函数的实际调用次数,
# 验证"最大重试次数"与"非指定异常不重试"等行为
pytest-mock>=3.11.0,<4.0.0
